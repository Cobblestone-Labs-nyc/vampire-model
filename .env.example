# Copy to .env (never commit .env). Mirrors the live droplet's /var/www/christopher/.env.
FAL_KEY=
FAL_API_KEY=
# Optional, only if a future caption/QA step uses it:
OPENAI_API_KEY=

# Local Stage-1 endpoint base, used by teacher_generate.py --teacher local and eval.py
STAGE1_URL=http://127.0.0.1:8008
